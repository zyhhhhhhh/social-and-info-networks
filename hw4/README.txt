The main file is yzhng127.py

The only thing that need to be changed is the last line where I called the main function.

If the input is "input.txt", change the "network.txt" to "input.txt" and run the code.